package main

import (
	"fmt"
	"go_everything/Go/Basic"
)

func main() {

	var name = "minhaj"
	const name1 = "sadik"
	profession := "a little programmer"
	fmt.Println(name, name1, profession)

	// output := Algorithm.SequenceLength([]int{4, 5, 4})
	// fmt.Println(output)

	// output = Algorithm.SequenceLength([]int{4, 12, 5, 4, 3, 5, 4, 3, 8})
	// fmt.Println(output)

	// Basic.Struct()
	// Basic.Switch()
	// Basic.Loop(fn, 23)
	Basic.Pointer()
	// Basic.Variable()
	// Basic.Slice()

	// advance.Map()
}
